{
  "name": "edunexus-monorepo",
  "version": "1.0.0",
  "description": "EduNexus School Management System Monorepo",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "npx concurrently -k -p \"[{name}]\" -n \"Server,Client\" -c \"yellow,blue\" \"npm run server\" \"npm run client\"",
    "seed": "npm run seed --prefix server"
  }
}
